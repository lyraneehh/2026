Difference between oauth2 vs odic

== How would you secure your exchange? ==

1) Public edge protection
2) API security
  - https
  - use OAUTH2, federated access
  - WebAuthn (fingerprint, who you are and what you have)
  - keys with various levels - read key for read access, write key for write, admin key for admin, instead of using master key. Keys expire
  - RBAC
  - Rate limiting (1 IP only 20 hits per min, free tier only 1k per day) both per request, and per type of request (for example, there would be more read request than write, so perhaps only 1k read but 500 write)
  - API versioning (include versioning number in url endpoint, so /api/v1/trade, /api/v2/trade, so we can deprecate versions we dont need
  - allow list (principal of deny all) - only specify IP range to hit certain endpoint
  - check OWASAP for API risk
  - use an api gateway
  - proper error handling, never return full stack error messaging
  - input validation (both done on client and server)

3) Business logic
4) Internal network segmentation
5) Identity nd Access management
6) Wallet and custody security
7. Database and data protection
Secrets and key management
Secure SDLC and CI/CD
Monitoring, detection, and incident response
Third-party and supply-chain security
Resilience and availability


Outside --> make request to bybit.com --> hit CDN --> hit WAF ---> load balancer --> actual web server.
- bybit.com should never give its IP, you should give the CDN ip.
- CDN will allow for rate limiting, cachche, bot prevents, risky users (using known list of bad IPs/Tor nodes/Proxy)

- WAF prevent: xss, sql, directory traversal, sensitive file path, command injections, configure to add known bad payloads

From web server --> App server
- each api request should be injected with a secret header (x-forward-header, x-secret-internal),
- security groups, iptables, only allow traffic to/from web server.
- mTLS to ensure if an a server in the network gets compromised and attacker runs tcpdump, they dont steal the secret header and do some sort of SSRF






====== Gateway vs FW ===

Gateway filters on the application layer such as:

IF API path is not allowed
OR HTTP method is not allowed
OR token is missing/invalid
OR user role is not allowed
OR rate limit exceeded
OR request body is invalid
OR risk score is too high
THEN reject


============================ AI trends in cyber security ==============================================================

============================ AD ======================================================================================
Different attacks, kerberosting, asrep, SPN

============================ docker forensics ======================================================================================

layers


============================ Windows =========================================================================================
Evidence of execution
- prefetch (shows first and last execution, up to 8 times)
- BAM 
- UserAssist (shows app name, run count, focus count)
- RunMRU (Windows+R)
- LNK files (there is creation timestamp, and target timestamp)
- RecentDocs (Tracks files and folders opened by the user.)

How are files stored in windows (MFT, USNjournal)

Evidence of exfiltration.
- SRUM (updates every hour, logs ttoal bytes sent per exe.

but for the most part, you got to tell the story:
We see some files being accessed, this can be confirmed through recentdocs, lnkfiles, prefetch files, and then 
we observed file creations, in the form of a zip file, and then we observe through DNS logs, browser history.

Usually exfiltration, there is a lifecycle: Attacker 
1) Access the files 
2) Stage the files (mft/usnjournal records shows around time of files being accessed, any .zip files created?) Can check deleted too
3) Exfiltrate the files (check firewall logs, dnscache (voltitle), powershell logs (would show commands executed), 
Security event logs (to see what process was being spawned, curl? wget? rcylone? winscp?) 
sysmon logs (show process creation, network connection, dns query,
SMB logs (Network share accessed)


Persistency
- run, runonce
- winlogon
- shortcut file in startup folder
- services, schtasks
- WMI event subscriptions (subscriptions have trigger and actions). Generayll wanna search for CommandLineEventConsumer class, 
WMI creation event logs: syslogs, event logs, C:\Windows\System32\wbem\Repository\ 

- DLLs through search order hijacking
- extensions + registry policy change
- creating accounts


How does lateral movement look like
- windows event logs with logon types (smb, rdp, winrm, pass the hash, 

Browser forensics, chrome, edge, firefox etc
- chrome/edge: 
get history.db, look for the urls, visit_source, transition types etc, dump it in sqlite 
get cookies (dont directly prove page visit, but supports it)
get cache (but hard to interept)
- mozilla: places.sqlite (history),

sysmon vs windows event
- window event logs are more for authentication related and has been there since the start
- sysmon are more for threat hunting, more granula, used commonly in edr ingestion

What artifacts can show PowerShell execution?
- ConsoleHost_history.txt
- your same few artifacts,

Can DLLs run on their own? no, they need an .exe

only services.exe, spawn svchosts.exe, anything else likely malicious

Volatile:
- netstat (network)
- process list / tasklist
- clipboard contents/

============================ Linux ==============================================================
How to mount a filesystem and analyze 

Evidence of execution:
- if you have access to volatile data and the scope was in the last few days, ausearch -x (bash, wget, curl etc) shows files that were recently accessed.
- stat (although not so good to find evidence of execution, but rather evidence of staging/creation
- 

Volatile:
- Process Memory Maps (/proc/<PID>/exe & cmdline

How are files stored in linux

evidence of exfiltration

How does lateral movement look like

Different logs

if a compromised host connects to another host, from the compromised host, besides known_host, what are other ways it check where the compromised 
host connected to

what volitile data to collect:
- ss/netstat (active sockets/network connections)
- process environment/command lines (proc) or running process ps -aux
  - cmdline, environ shows execution arguments
  - copy evertyhing from /proc/[0-9]*/
-  open FD (lsof) and deleted binaries (ls /proc/*/exe | grep deleted)



============================ General ============================================================

what voliatile memory to collect


What is process injection?
What is DLL sideloading?

TLS handshake

How DNS works

SPF, DKIM, DMARC, ARC


Walk me through how you'd triage an incident
- Cloud incident
- CICD 

============================ Container breakout ===========================================================

============================ Kubernetes =======================================================

we use kubernetes when we want to deploy the application, whether its in a testing or production enviornment.

And the purpose of kubernetes,

i mean what are some issues you face when you're deploying or maintaining an app,

- the most obvous one is; hey my server goes down, gotta start it up, you gotta find which one it is, 
what. caused the server to go down, and then most important, spin a new server,


and how kubernetes solves this is through desired state, you state, through a yaml file, 
hey if something goes down, spin a new instance, with the same number of pods, and oh and every few minutes, 
i want you to make a request to myself and see if it can receieve, if i cant, that means im down. This is self healing


next: Reousrce utilization: say you have 4 servers, its expensive to run servers, and all servers are running at 10% utlization ( i mean if there is
not a lot of traffic = not a lot of utiliazation = still waste of money

kubernetes solves this by saying ok, if traffic is low, remove all the rest of the instances, and only use 1, if traffic is more, up the instances.

So then next thing it solves is, since it tries and reaches desired state, that means pods or instances keep dying, so how do users know what is the 
IP of the website. It does so through something called services. there can be 1 or more services, in an instance, and each instance
can have 1 or many pods. and say you kill a pod that is the front end to the website, and therefore the IP along with the pod goes with it.
how can users still access that pod? its because it uses the service's IP.

So then next thing it solves is, since it tries and reaches desired state, that means pods or instances keep dying, so how do users know what is the 
IP of the website. It does so through something called services. there can be 1 or more services, in an instance, and each instance
can have 1 or many pods. and say you kill a pod that is the front end to the website, and therefore the IP along with the pod goes with it.
how can users still access that pod? its because it uses the service's IP.

How is kubenetes attacked?
- exposed pod, -> access/curl service token (/var/run/secrets/kubernetes.io/serviceaccount/token) --> RBAC permissions allow for
creating pods, list secrets, access other pods, generate long lived auth tokens for other serviceaccounts.
FYI creating pods = attacker creates new yaml file, uses stolen service token to run new yaml file to create pod, then set priv=true to get overly priv pod
- supply chain attack, cicd pipeline compromised --> inject malicious binary/reverse shell into container file ssytem during build stage
- Exposed kube-api server 

============================ Security architecture ===========================================================

What would you secure your architecturec- load balacners, firewalls, etc


============================ You observed Crypto-miner application running on an instance?  =======

Immediately:

check if instance is publicly accesible: Exposed ports, services etc
check what has the ability to run instances?
  
  - User account (How did it get stolen, bruteforce? no mfa, supply chain attack gain access to stolen creds in build runner?)
  - Instance
  - CICD Pipeline
  
  - are there any CICD pipelines that run instances?
        - Golden image compromise, container registry compromised? 
        - 


============================ How to determine how an instance was launched? =======


============================ What are some considerations when performing evidence acquisitions? =======

When performing evidence collection, one key consideration is where the evidence is being collected from: bare metal, laptop, physical server, cloud VM, or VPC environment.
In a cloud/VPC environment, we may be able to collect evidence by taking a cloud snapshot/image of the VM’s attached disk volumes from the cloud account. 
This is different from bare metal collection, where we may need to physically access the disk, use forensic imaging tools, or collect from the live operating system.

We also need to consider if LVMs are being used. LVMs are logical storage areas on top of disk or partitions. So when running lsblk, one would typically see /dev/sda1,sda2, or /dev/vda1,vda2. These are partitions.
Of that partition, assuming lVMs are being used, one would see a LVM (physical volume - which its ironic because its not physical, since its on top of something logical, hence its a logical physical volume),  then carved out is volume group, 
which is the name of the volume, and then the actual logical volume. 

============================ Case study ====

While creating new user accounts in Active Directory, you find that several legitimate user accounts with no apparent connection are part of an
undocumented group named "test". 

What do you look for to investigate whether an incident occurred? Focus on the efficiency of your investigative actions here. 
--
You believe a Linux server was used as a jump box to pivot into another network segment, but the network traffic would not have crossed a sensor boundary for logging.

What evidence do you look for to prove the belief?
--
A user reports their hard drive is full, but they don't know why. While investigating, you find a series of large, password-protected RAR files that the user knows nothing about. 

What do you look for to investigate whether an incident occurred?
--
You've discovered a host with multiple instances of Chrome running the --hidden option. 

What do you look for to investigate whether an incident occurred?
--
Browser history for an HR user shows repeated visits to chat.openai[.]com, followed by creation of C:\Users\chris\AppData\Local\Temp\cleanup[.]ps1. The file is not available, and the hash shows no matches in OSINT resources.

What do you look for to investigate whether an incident occurred?
--
You find Event ID 7045 showing a new service installed: WinUpdateCheck, pointing to C:\ProgramData\wucheck.exe. You report to the SOC lead that this system is infected and needs to be contained. 

They ask you to justify that request. 

What evidence do you present to elevate this from “suspicious service creation” to confirmed malicious activity? Lead with your strongest likely evidence sources and conclusions. 
--
A user reports OneDrive crashing on startup. You see OneDrive.exe launched as expected, but then you spot conhost.exe spawned within 2 seconds, followed by mshta.exe -- no obvious error dialogs.

What do you look for to investigate whether an incident occurred?
--
You know an attacker accessed several customer support workstations in the past month based on discovery of a consistent persistence mechanism. You suspect wider access, but auth logs only go back 24h. 

How can you determine where else the attacker went?
--
You can pull any five forensic artifacts from a HDD and are limited to those, which do you pull and why?

Keeping it intentionally vague to see what clarifying questions they ask if any, and to see their thinking 
--
“tell me what happens when you visit a website” and my immediate response is always, “I can speak on this at length, do you want me to go into detail and frame it using all the layers of the OSI model?” They usually respond “yes,” and I usually get cut off on layer 5 for the next question.
--
